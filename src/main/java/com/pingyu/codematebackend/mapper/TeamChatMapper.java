package com.pingyu.codematebackend.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.pingyu.codematebackend.model.TeamChat;

public interface TeamChatMapper extends BaseMapper<TeamChat> {
    // 暂时不需要手写 SQL，MP 够用了
}